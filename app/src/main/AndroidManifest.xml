<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project_1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon_app_2"
        android:label="Quản Lí Chi Tiêu"
        android:roundIcon="@mipmap/ic_icon_app_2"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_1">
        <activity android:name=".HomeActivity"
            android:theme="@style/Theme.Project_1.NoActionBar"></activity>
        <activity android:name=".Change_PassWordActivity" />
        <activity android:name=".Sign_UpActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>